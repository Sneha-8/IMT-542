# Copy to .env (never commit .env)
# Yelp is OPTIONAL — OpenStreetMap works without any key.
# Get Yelp key: https://www.yelp.com/developers/v3/manage_app

YELP_API_KEY=
SBP_API_TOKEN=dev-class-token
USE_MONGITA=true
SBP_DEFAULT_LOCATION=Seattle, WA
