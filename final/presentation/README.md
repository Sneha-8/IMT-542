# Final project presentation

## HTML (recommended for class)

Open in Chrome or Edge (fullscreen with `F`):

```bash
open /Users/snehareddy/Desktop/IMT-542/final/presentation/index.html
```

Or from repo root after clone:

```bash
open final/presentation/index.html
```

**Controls:** arrow keys or space to advance; `Esc` for overview; `F` fullscreen.

**Live demo links:** start Flask + ngrok, then open:

```text
final/presentation/index.html?api=https://YOUR-ID.ngrok-free.app
```

That rewrites demo links to your public API.

## PPTX (Canvas / GitHub requirement)

- File: `IMT542_Final_Project.pptx`
- Regenerate: `python scripts/build_presentation.py` (from `final/` with venv active)
